{
  "version": 2,
  "builds": [
    {
      "src": "api/webhook.php",
      "use": "@vercel/php"          // ← no version tag → Vercel pulls the latest
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api/webhook.php"
    }
  ]
}
